import 'package:flutter/material.dart';

class LazyLoadListPage extends StatelessWidget {
  const LazyLoadListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
